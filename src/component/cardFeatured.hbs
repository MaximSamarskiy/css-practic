{{#with data}}
<div class="card">
    <div class="card_photo" style="background-image: url({{photo}});"></div>
    <h2 class="card_title">{{heading}}</h2>
    <p class="card_info">{{info}}</p>
    <a href="{{linkUrl}}" class="card_linkText">{{linkText}}</a>
</div>
{{/with}}